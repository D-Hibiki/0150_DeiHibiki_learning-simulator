# 貢献ガイド

## 基本方針

このプロジェクトは、現実の社会調査ではなく合成モデル内の計算実験です。変更によって、結果が因果効果、母集団代表値、将来予測、実証済み教育効果に見えないようにしてください。モデルや統計表現を変える場合は、コードだけでなく `docs/METHODOLOGY.md`、画面上の注意書き、出力メタデータ、テストを同時に更新します。

## 開発開始

```powershell
npm ci
npm run dev
```

依存関係は`package-lock.json`をSSOTとして再現します。依存関係を変更する場合は、採用理由、公式仕様、既存機能で代替できない理由、セキュリティ勧告を確認してください。

## 変更の進め方

1. 変更の目的、適用範囲、対象外をIssueまたはPR説明に記録する。
2. モデル変更では、先に期待する性質と失敗条件をテストへ追加する。
3. UI変更では `docs/design/reference.png` と既存トークンを基準にする。
4. 実装後に `npm run verify` を実行する。
5. 自動化できない確認項目をPRへ列挙する。
6. 人の目視承認を得るまで公開範囲を拡大しない。

## 必須検証

```powershell
npm run verify
```

このコマンドに含まれる検査は次です。

- TypeScript型検査
- VitestによるUI・モデル・統計・レポート検査
- Viteプロダクションビルド
- Sites Workerとパッケージ成果物の契約テスト

依存関係を変更した場合は、追加で`npm audit`を実行してください。

## モデル変更のレビュー項目

- 同じモデル版、Seed、設定で完全に再現できるか
- 1試行内の3方式が同じ合成コホートを参照するか
- 反復数を増やしても既存の乱数系列prefixが変わらないか
- 入力範囲、人数配分、下位層抽出、統計式が文書と一致するか
- `max`による併用方式の構造的優位を明示しているか
- モンテカルロ誤差と、モデル・入力・外的妥当性の不確実性を区別しているか
- 出力JSON/CSVにモデル版、Seed、PRNG、入力、限界が残るか

## UI変更の人間確認

- デスクトップとモバイルのレイアウト、文字切れ、水平overflow
- シナリオ、数値入力、スライダー、反復、進捗、中止、タブ遷移
- JSON/CSVのネイティブ保存と文字コード
- 印刷/PDF表示
- 注意書きが主要結果と同じ画面で認識できること
- ブラウザーコンソールに新しい警告・エラーがないこと

## セキュリティとプライバシー

秘密情報、個人データ、実測の参加者データを追加しないでください。認証、API、分析、データベース、永続化、外部送信を追加する変更は現在の境界を変えるため、実装前にデータフロー、脅威モデル、運用、削除・保持方針、承認点を設計してください。

脆弱性は公開Issueへ投稿せず、[SECURITY.md](SECURITY.md)の非公開報告手順に従ってください。

## PRの完了条件

- 変更範囲が明確で、無関係な変更が含まれない
- `npm run verify`が現在のHEADで成功している
- 方法論、アーキテクチャ、脅威モデル、運用文書が実装と一致する
- 自動テストで確認できない項目と、その確認者が明記されている
- Sites previewまたは公開が必要な場合、private-by-defaultとし、外部操作ごとの承認を得ている
- rollback対象となる直前の保存済みSites versionを特定できる
